import {PrismaClient} from '@prisma/client';

const prisma = new PrismaClient();

export const updateJobSeeker = async (req,res) => {
    const {userId} = req.params;
    const {name,salaryExpectation}
    try {
        
    } catch (error) {
        
    }
}